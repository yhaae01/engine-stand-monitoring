import { TransformTitleCasePipe } from './transform-title-case.pipe';

describe('TransformTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
